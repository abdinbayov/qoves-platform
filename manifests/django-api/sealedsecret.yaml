# Generated by: kubeseal --format yaml < /tmp/db-secret.yaml > manifests/django-api/sealedsecret.yaml
# The encryptedData values below are ciphertext — safe to commit.
# The plaintext DATABASE_URL never touches git.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: django-api-db-credentials
  namespace: django-api
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
spec:
  encryptedData:
    DATABASE_URL: REPLACE_WITH_KUBESEAL_OUTPUT
    SECRET_KEY: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: django-api-db-credentials
      namespace: django-api
    type: Opaque
