# Generated by: kubeseal --format yaml < /tmp/pg-secret.yaml > manifests/postgres/sealedsecret.yaml
# The encryptedData values below are ciphertext — safe to commit.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: postgres-credentials
  namespace: postgres
spec:
  encryptedData:
    username: REPLACE_WITH_KUBESEAL_OUTPUT
    password: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: postgres-credentials
      namespace: postgres
    type: Opaque
